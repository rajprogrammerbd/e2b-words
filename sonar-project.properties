sonar.projectKey=rajprogrammerbd_e2b-words
sonar.organization=rajprogrammerbd
sonar.javascript.lcov.reportPaths=./coverage/lcov.info
# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=e2b-mailer
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
sonar.tests=./src
sonar.test.inclusions=./src/**/*.test.ts,./src/**/*.test.tsx
sonar.language=js,jsx,ts,tsx

sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/*.stories.tsx,.git/**,**/__tests__/**,**/newrelic.ts**

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.verbose=true